import { HTMLAttributes } from 'react';

type Props = {
  watchlist: Watchlist;
} & HTMLAttributes<HTMLDivElement>;

const WatchlistTab = (props: Props) => {
  return (
    <div {...props} className="h-10 rounded-lg bg-white border border-neutral-300 flex items-center">
      {' '}
      {props.watchlist.name}
    </div>
  );
};

export default WatchlistTab;
